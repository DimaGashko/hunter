'use strict';

describe('Класс Game.Gravity', () => {
  describe('Метод use()', () => {

    /*it('Добавляет вектор притяжения к вектору скороти объектов', () => {  
      var gravity = new Game.Gravity({
        g: new Vector(1, -10),
      });

      var actor1 = {
        speed: new Vector(100, 200),
      }

      var actor2 = {
        speed: new Vector(-80, -20),
      }

      gravity.use([actor1, actor2]);
    
      assert.strictEqual(actor1.speed.x, 101);
      assert.strictEqual(actor1.speed.y, 190);

      assert.strictEqual(actor2.speed.x, -79);
      assert.strictEqual(actor2.speed.y, -30);
    });*/

   
  });

  
});
